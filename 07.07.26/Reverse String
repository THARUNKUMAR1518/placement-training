class Solution {
    public void reverseString(char[] s) {
        int l=s.length;
        char a[]=new char[l];
        int k=0;
        for(int i=l-1;i>=0;i--){
            a[k]=s[i];
            k++;
        }
        for(int i=0;i<l;i++){
            s[i]=a[i];
        }
    }
}
